import { StyleSheet, Text, View } from "react-native";

import * as Components from "../components/_index";

const LoginScreen = () => <Components.AuthContent login />;

const styles = StyleSheet.create({});

export default LoginScreen;
